"""
Sign up for an app with twitter.
Get API keys for OAuth access w/ Read-only permission.
"""
keys = dict(
    consumer_key = '',
    consumer_secret = '',
    access_token = '',
    access_secret = '',
)
preferences = dict(
    max_results = 100,
    db_filename = 'tweets.db',
)